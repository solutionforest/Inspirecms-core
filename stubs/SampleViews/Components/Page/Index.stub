@php
    $locale = $content->getLocale() ?? request()->getLocale();
@endphp
<x-layout :title="$content->getTitle()" :seo="$content->getSeo()?->getHtml()" :locale="$locale">
    <x-header :locale="$locale" />

    {{ $slot }}

    <x-footer :locale="$locale" />
</x-layout>